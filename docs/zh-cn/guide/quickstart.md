# 1.全局安装 docsify-cli 工具，可以方便地创建及在本地预览生成的文档

```bash
npm i docsify-cli -g
```

# 2.初始化项目

```bash
如果是在docs目录下写文档，可以自己定义文档所在目录
docsify init ./docs
```

# 3.运行启动一个本地服务器，可以方便地实时预览效果(默认端口是 3000)

```bash
docsify serve docs
```

> 想了解更多关于 docsify 的知识可以在官网进行查看:https://docsify.js.org/
